# Copy to .env — never commit .env with real values
# WARNING: All VITE_* variables are embedded in the browser bundle. Do NOT put secrets here.

VITE_API_URL=http://localhost:5500/api/v1
VITE_HEAP_ID=
VITE_ALLOWED_EMAILS=
VITE_CHATBOT_STREAM_ENABLED=false
